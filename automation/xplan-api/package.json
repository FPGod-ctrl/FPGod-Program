{
  "name": "xplan-api-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP connector for the XPLAN (Iress) Custom API — OAuth2 + Risk Researcher quoting tools.",
  "scripts": {
    "login": "node src/login.mjs",
    "whoami": "node src/whoami.mjs",
    "mcp": "node src/server.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  }
}
